{
  "name": "default",
  "version": "0.0.0",
  "description": "default",
  "scripts": {
    "setup": "npm install && node -e \"require('skedge-pack')({skeleton:true});\" ",
    "dev": "node -e \"require('skedge-pack')({ flags: 'server sourcemaps watch' });\" ",
    "prod": "node -e \"require('skedge-pack')({ flags: 'minify test full' });\" "
  },
  "author": "Skedge Me",
  "license": "ISC",
  "dependencies": {
    "immutable": "^3.8.0",
    "react": "^15.0.1",
    "react-dom": "^15.0.1",
    "react-redux": "^4.4.5",
    "redux": "^3.4.0",
    "skedge-pack": "github:skedgeme/skedge-pack#v0.9.5"
  }
}
